# Peripheral Access Crate `${crate}` for ${chip} 32-bit MCU from Atmel/Microchip

This crate contains autogenerated code to access peripherals of ${chip} generated from official SVD files from Microchip website using [svd2rust](https://github.com/rust-embedded/svd2rust/) tool (version ${svd2rust_version}).

SVD for this chip is sourced from [official Microchip repository](http://packs.download.microchip.com/), package version ${atpack_version}.
